[pytest]
# Exclude backend tests from the local agentshield package test run.
# The backend is a separate service for the full platform.
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
